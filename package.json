{
  "name": "cullumi-ui-tests",
  "private": true,
  "scripts": {
    "test": "npm run test:dom",
    "test:dom": "playwright test --config tests/dom/playwright.config.mjs"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
